//###########################################################
//			MEMO
//ÉtÉåÉìÉhìoò^É_ÉCÉAÉçÉOópÉvÉçÉOÉâÉÄ
//###########################################################

package com.example.j14234.project_v5_2;

import android.app.AlertDialog;
import android.app.Dialog;
import android.app.DialogFragment;
import android.content.DialogInterface;
import android.os.Bundle;

public class MyDialogFragment extends DialogFragment{
    private int check = 0;
    @Override
    public Dialog onCreateDialog(Bundle savedInstanceState){
        AlertDialog.Builder builder = new AlertDialog.Builder(getActivity());

        final String resultName = getArguments().getString("resultName");
        //final int resultId = getArguments().getInt("resultId");

        return builder.setTitle("ÉÜÅ[ÉUÅ[ìoò^")
                .setMessage(String.format("%sÇ≥ÇÒÇìoò^ÇµÇ‹Ç∑Ç©?",resultName))
                .setPositiveButton("ÇÕÇ¢",new DialogInterface.OnClickListener(){
                    public void onClick(DialogInterface dialog, int which){
                        //Toast.makeText(getActivity(),"ìoò^ÇµÇ‹Ç∑",Toast.LENGTH_SHORT).show();
                        check = 1;
                        UserSearch callingActivity = (UserSearch)getActivity();
                        callingActivity.onReturnValue(check);
                    }
                })
                .setNegativeButton("Ç¢Ç¢Ç¶",new DialogInterface.OnClickListener(){
                    public void onClick(DialogInterface dialog,int which){
                        //Toast.makeText(getActivity(),"ìoò^ÇµÇ‹ÇπÇÒ",Toast.LENGTH_SHORT).show();
                        UserSearch callingActivity = (UserSearch)getActivity();
                        check = 0;
                        callingActivity.onReturnValue(check);
                    }
                })
                .create();
    }
}
